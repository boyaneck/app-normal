const makeContext = async (roomName, event, sampe) => {
  const [chat, reference, hostMetaData] = await Promise.all([
    (makeChatContext = roomName),
    makereference(event, sample),
    getHostMetaData(roomName),
  ]);

  return {};
};
